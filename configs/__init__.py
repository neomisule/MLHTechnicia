"""
Configuration modules for different agent types.

Each config file contains:
- Model settings
- Agent instructions for each component (atomizer, executor, verifier, planner, aggregator)
- Tool configurations
- Strategy configurations
- Memory configurations
"""

